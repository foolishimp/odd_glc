# odd_glc Governance

**Status**: Proposed exact-tree governance candidate; not yet admitted
**Target status**: Ratified method selection only after the exact-tree gate below
**Effective scope**: current `odd_glc` source-project work
**Selected method Product**: STDO `2.3.0`

## Immutable Method Basis

`odd_glc` selects the released STDO Product identified by all of the following:

| Coordinate | Exact value |
|---|---|
| annotated tag | `v2.3.0` |
| tag object | `a0a9a177bb1e835597517a55e25d231617d8eaef` |
| commit | `b2c64047e01d9d582243a25af587ac772233a4ea` |
| tree | `7734ac10ae4fe924e6d406297a7f1984ab7451e7` |
| Product members | exact 41-member `specification/standards/` distribution |
| standards aggregate SHA-256 | `014ead42a96412f51a172a8158e42fdfe1447a0af5e4f70a79cf8a020129630c` |
| release note | `releases/v2.3.0.md`, SHA-256 `a1025340fee98a93fc9a08c969fcaf4f161628d433647c6b9c8b4c9598f4241e` |

The selected normative members are read from that immutable tag, not from the
mutable `specification_methodology` checkout. For current odd_glc work, the
primary routed members are `SPEC_METHOD.md`, `DESIGN_MODULE_METHOD.md`,
`ODD_METHOD.md`, `RELEASE_METHOD.md`, `TICKET_METHOD.md`, and
`WRITING_GUIDE.md`; the rest of the selected 41-member distribution applies
when its subject is in scope. Local summaries, compressed views, installed
context blocks, comments, tickets, and sibling-repository precedent do not
replace those selected bytes.

| Primary selected member | SHA-256 at `v2.3.0` |
|---|---|
| `specification/standards/SPEC_METHOD.md` | `25e673f16a1d28fc43c95848a486625b34da8ea703ad36febb653456e86f1b37` |
| `specification/standards/DESIGN_MODULE_METHOD.md` | `82e40cf5074b1af0ba05c459c664c29a625cba8fa07c9b791b31973cf0c725da` |
| `specification/standards/ODD_METHOD.md` | `42be18b4d16e7d1bd8ca764d951fd533a19fa0ce4c67d17462c3a0f6879d46bb` |
| `specification/standards/RELEASE_METHOD.md` | `f66805ccaab8bbb51a2dcd8309dc121780a994230194067f42eaa0a280cc184e` |
| `specification/standards/TICKET_METHOD.md` | `2a23614cdd344179cf43263dd6074b1c2f7d4747c33083fdcebff3992df41675` |
| `specification/standards/WRITING_GUIDE.md` | `8af7a15e1a9f679dd7327a7da32c915e28a2f1fd0d0048d8292e09276968b8c7` |

## Local Authority Order

Authority flows in this order:

```text
Goals -> Intent -> Product Definition -> Requirements -> ratified Design
  -> Code -> admitted Events -> Projection -> Delta -> Scenarios
  -> Gap Analysis -> Repricing
```

- `specification/GOALS.md` selects the bounded current work wave.
- `specification/INTENT.md` and `specification/PRODUCT.md` govern Product
  direction and meaning.
- `specification/requirements/` governs required behavior and boundaries.
- active entries in `build_tenants/common/design/README.md` govern realization.
- `.ai-workspace/tickets/` routes work; it does not create Product or design law.
- `.ai-workspace/comments/`, tests, generated context, and implementation
  precedent are evidence only.

The source project, a release cut, a released Product, an install, and a built
artifact are distinct subjects. A development Product used as substrate does
not become part of the mutable `odd_glc` source project.

## Directly Adopted Local Construction Method

Direct F_H adoption on 2026-08-04 selected the local construction method
named **authority-conserving, entity-centric, event-sourced functional
reactive domain modeling**. Its exact provenance is:

| Coordinate | Exact value |
|---|---|
| adopted source | `20260802T032426Z_MANIFESTO_driving_ai_authored_coding_from_programming_basics.md` |
| source commit | `1f6a86074bf995763b4caff286422b5b1501374b` |
| source SHA-256 | `dff495762dfacaaa20b095d146d6afa4a969e29d4f385b5884272d50ad17e153` |
| adoption authority | direct F_H |

The named source labels itself commentary. Direct F_H adoption, not that
label, selected the bounded method for `odd_glc`. The source commit was not
resolvable in the reviewed source checkouts. Its identity and digest therefore
remain explicit, while this local governance surface retains the operative
rules needed to reconstruct the adopted method without that checkout. A later
resolvable copy may corroborate those bytes; it may not silently alter them.

The local method is subordinate to the selected STDO Product and to
`odd_glc` Goals, Intent, Product, requirements, and admitted design. It governs
construction from already-authorized computational relations. It cannot
change Product meaning, admit a transformation, accept a release, weaken an
STDO gate, or make a proposed design operative.

For every edited function, the coding plan identifies:

```text
entity
+ lifecycle transition or projection
+ functional owner
+ registered common-library relation
+ selected lawful technology and role
+ exact input authority
+ exact output carrier
+ prohibited calls and state
+ direct consumers
```

Construction follows:

```text
entity and lifecycle map
  -> common-block and technology selection
  -> donor and external implementation review
  -> proportional design delta
  -> coding plan
  -> code
  -> code review
  -> module and composition tests
  -> accept
  -> next slice
```

The operative rules are:

- start from entities, identities, lifecycles, transformation law, admission,
  durable events, projections, consumers, and prohibited transitions;
- use immutable values, pure transition/projection functions, admitted event
  history, Event Calculus currentness, typed owner adapters, and narrow effect
  shells;
- open only the recursive functional frame needed to close the parent
  contract;
- search the affected dependency cone before adding a helper or dependency;
- classify each algorithm as reuse, cohesive extension, or justified new
  common-library relation;
- keep common algorithms authority-neutral and Product meaning in typed owner
  adapters;
- use only selected technology in its declared role; and
- prove composition and fresh-process reconstruction proportionally to the
  affected cone.

The method's singular-owner language is read through STDO transformation-
admission completeness: singularity belongs to the complete admission
relation, which may include declared quorum, delegation, human step-up, or
other lawful constituents. The local method does not narrow the governing
constitution.

## Re-entry And Construction Law

Every substantive change declares the smallest lawful re-entry point:
`goal_reprice`, `intent_reprice`, `product_reprice`, `requirement_reprice`,
`design_reframe`, or `realization_refactor`. Missing upstream substrate is named
and blocks the dependent cut; it does not authorize an odd_glc substitute.

For ODD-shaped work, odd_glc owns domain vocabulary, Program and GraphFunction
declarations, domain policy, specialization, and proof interpretation. GTL owns
the public declarative language. ABIogenesis Product/Validator owns
verification, resolution, install, workspace, and Catalog relations. HoG/ABG
owns execution, admission, events, Event Calculus, replay, continuation, and
runtime projection. Technology availability or package co-location grants no
semantic authority.

An accepted design must expose enough durable domain, sequence, state,
authority, readiness, and computational relations to reconstruct every
governing invariant without conversation history or private implementation
knowledge. Agentic construction may make only bounded local repairs inside the
accepted causal set. Product or release acceptance remains an exact-subject
decision.

## Exact-Tree Admission Gate

This document and the T-041 design in the same candidate tree are proposals;
their wording does not admit or ratify that tree. They become operative only
if one immutable candidate tree, with direct parent
`96b5a9c2e109cb584f943f79997ac213d069f411`, satisfies every condition below:

1. the complete candidate tree is frozen and identified by its Git tree hash;
2. independent reviewers assess that same tree without amendment;
3. Executive/F_H explicitly accepts that exact tree, not a branch, worktree,
   mutable diff, agent report, or approximate file set;
4. the accepted tree is committed unchanged, with the direct parent above;
   if a proposal was committed before acceptance, a later status-only
   admission records the transition instead of retroactively claiming this
   proposal was already ratified; and
5. the accepted commit/tree disposition is recorded durably outside the
   candidate tree or in a later status-only admission, never as a self-
   acceptance claim in the candidate commit message.

Content changes after review create a new tree and restart the gate. Agent
identity, dirty-checkout state, and uncommitted-candidate identity are not
durable admission coordinates.

## T-041 Instantiation

T-041 entered through direct F_H `goal_reprice`; its design-completion cut is a
bounded `design_reframe`. It does not change `INTENT`, Product meaning, or an
active requirement. If admitted, the selected zero-code Program-only Hello
sentinel proves one install/catalog/execution/replay seam. It does not delete or close the
broader Product-owned read-only lifecycle interpretation described by
`specification/PRODUCT.md`.

The absence of this file at predecessor commit
`96b5a9c2e109cb584f943f79997ac213d069f411` was an integrity and routing defect,
not authority to adopt mutable methodology. The exact STDO release coordinates
above make the repair deterministic and bounded. Prior governance text,
comments, tickets, and dirty-checkout content remain evidence only and do not
self-ratify this selection or the T-041 design.
