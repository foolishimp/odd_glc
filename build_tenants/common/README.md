# odd_glc Common Tenant Surface

Common build-tenant material applies across future realization tenants.

This folder is design-only until the tenant registry activates a concrete
realization line. It shall not contain runtime code, generated install state,
or local ABIogenesis wrappers.
