# ABIogenesis Recursive Span Identity Proof

This proof input is a committed ABG live replay artifact for span lineage, frame,
zoom, foldback, and re-entry truth.

`odd_glc` consumes it as read-only ABG recursion truth. It does not open frames,
derive span identity, fold spans back, or route graph re-entry.
