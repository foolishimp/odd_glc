# ABIogenesis Non-Closed Route Mechanics Regression

This fixture is a committed ABG installed replay artifact for non-closed
requirements-route mechanics.

`odd_glc` consumes it as read-only ABG replay truth. It does not emit residuals,
select continuation, route re-entry, or construct disposition truth.

This proof input is not live execution-grounded. The upstream producer uses an
installed test evaluator stub for non-closure, so this input may guard read-model
mechanics only. The live non-closed route proof is the closure input for
downstream non-closed lifecycle interpretation.
