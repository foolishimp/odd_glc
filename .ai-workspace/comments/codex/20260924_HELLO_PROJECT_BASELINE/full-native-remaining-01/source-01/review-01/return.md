# CLOSED — remaining full-Hello caller review

Reviewer: gpt-6-astra / max. Product frame: GLC full-source lifecycle / T-043 / selected RC4, composed with ABG's fixed fifteen-family GOAL-035 / T-287 under STDO 2.5.1 RC1; end-to-end interface integration, Owner and Conservation. This checks the caller's preservation and use of existing owners, not a new lifecycle or a release gate.

**Satisfied for the frozen source slice; no actionable blocker found.** Reviewed [Worker return](../return.md) SHA256 `b647ecf08d28b2477ee36229b7d8289d38115155204700f2bea4fd35f74aaf9d`, [eight-path subject](../source-subject.json) SHA256 `cbbf51195934fc074169d617727cf1903d7d9ba69d7a633cea15922c2e23c068`, complete diff, original selected source declarations, five new oracles, focused checks and necessary current owner routes. All eight postimages still matched at conclusion. No source, package, Program or core change was made by this review.

The selection preserves each original declaration's exact bytes and digest, all eight historical stage surfaces, and its evaluator identity. Unknown/duplicate selections, changed source and crossed oracle identities refuse. The five cases retain their distinct obligations: JS module return and component/UAT; bootstrap artifact-authority relationships and historical witness; dependency-free Cargo execution; real loopback HTTP/port-file/clean-exit service behavior; and separate Hello/World branches with composed fan-in. Existing historical traversal and parallel-shape limitations remain explicit. The current native graph cannot be credited as the old eight-vector or ABG branch-frontier witness merely from these inputs.

Worker/evaluator separation is preserved. Original source expectations remain ordinary source meaning; the separately selected `evaluationData` does not become native author context. The existing semantic owner adds it on the assessor branch, while construction continues from independently assessed Design. `completeFullSandboxJob` passes the exact Product-constructed input and digest through ordinary conformance, resolution and invocation; no case key creates a producer identity or authority. Generic native Program/core/dev8 are unchanged.

The executable relation is wired through both preparation paths: `full-sandbox-support.mjs` passes configured capabilities into `constructOrdinaryJobInput` during fresh construction and retained reconstruction. They enter `job.worksiteScope`, which the existing `projectSemanticJobActorContract` exposes as Design scope. Existing `semantic_job.ts` command admission checks the exact executable, cwd roots, timeout/grace bounds and declared environment, including nested HTTP launch. Native C2 then retains the admitted construction's authority/binding/direct grant and exact observed target/dependency sources; `worksite_command_execution.ts` independently checks this join and command/predicate/write configuration. These scope restrictions do not manufacture a Public grant. Schema acceptance of the test's deliberately fictitious Cargo path establishes neither executable availability nor installed capability admission.

The CLI-only observational interpreter correctly returns `unexecuted_probe` / `outside_direct_cli_interpretation` for module-return, Cargo-command and HTTP oracles. It cannot turn a matching CLI string into their success. Actual admitted command predicates, verifier bodies and independent semantic assessment must determine each case's outcome. That is the existing duty, not an added test gate.

Reused the three successful syntax checks and seven focused tests in [checks.json](../checks.json); no test rerun or targeted falsifier was warranted. This is source/constructor evidence. It does not establish any of the five installed preparations, actual Rust/HTTP effects, model outcomes, fresh Public reads or application closure. Basic-cli's accepted outcome was not re-reviewed or enlarged.

Root's subsequent execution selection supersedes only the plan's proposed sequential order: **parallel independent cases**, each with its own fresh resource, worksite, binding, archives and receipts. Root reports 128 GiB RAM / 16 logical CPUs; this review did not remeasure capacity. Existing archive-reuse preparation retains core17/dev8 and the explicitly configured RC4 role environment; stock package-building preparation is not selected. Per case the declared ceilings remain CLI 2.1.280, `claude-opus-5-5` / xhigh, twelve one-attempt actor occurrences, $5 each ($60 nominal case / $300 five), 300s inactivity, 900s actor absolute, 1s grace, 5400s native, 6000s outer and 600s readback, default heap, no fallback or retries. They are ceilings, not predicted time/cost or proof of convergence. No new scheduler is needed. A real shared framework failure returns its first cause and last lawful state without an automatic repair/retry loop.

Review writes only this return. No journal read, replay, actor, package, build, broad suite or runtime effect occurred. Remaining evidence is the selected installed case execution and its existing outcome/readback interpretation, not another source-review campaign. No suite, Data Mapper, fifteen-family or RC1 qualification claim follows from this disposition.
